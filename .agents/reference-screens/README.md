# V-19 Reference Screens

Tracked screenshot files were intentionally removed from this repository during
the local main cleanup. Do not rely on `.png` files in this folder as source
truth.

For UI work, use:

- `.agents/rules/v19-screen-wireframes.md` for screen structure and acceptance
  criteria.
- Current runtime screenshots captured during the task for fresh evidence.
- Source code and shared tokens in `src/shared/ui/visual-baseline.css` and
  `src/shared/ui/system.css`.

If a future task needs visual references again, generate fresh runtime captures
under a task-specific temporary QA folder and avoid committing screenshot
artifacts unless explicitly requested.
