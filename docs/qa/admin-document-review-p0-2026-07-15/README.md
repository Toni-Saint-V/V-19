# Admin document-review P0 proof — 2026-07-15

This folder contains fresh local-demo screenshots from the real 390×844 UI flow:

1. open an admin review entry;
2. verify that `Файлы` is the immediately visible active tab;
3. click `Сверить` for the passport;
4. prove that confirmation is disabled when the protected original cannot be read.

The proof does not upload, accept, or otherwise mutate production data.
